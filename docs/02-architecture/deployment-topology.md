# Deployment Topology

Local development starts with CLI plus local files. Later phases add server, PostgreSQL, vector DB, and integration gateway under Docker Compose, then Kubernetes manifests for production.
