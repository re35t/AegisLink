# Agent Communication Flow

Agent A builds a signed message, attaches a capability claim, sends it to the server, passes policy validation, and the server routes it to Agent B. Agent B verifies the envelope before responding.
