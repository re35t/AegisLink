# Trust Boundary

Agent private memory remains local unless explicitly exposed by capability and policy. The server can route and audit, but it should not receive private payloads unless the selected deployment mode permits it.
